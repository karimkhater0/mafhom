import 'package:flutter/material.dart';

class STTScreen extends StatelessWidget {
  const STTScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('STT Screen')),
    );
  }
}